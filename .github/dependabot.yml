version: 2

updates:
  # Keeps the workflow's actions current - in particular the SHA-pinned
  # third-party release action, which no floating tag updates for us.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
